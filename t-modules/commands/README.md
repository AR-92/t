# Command Modules

This directory contains command execution functionality for the T file manager.

## Files

- `external.sh` - External command execution
- `custom.sh` - Custom command management (add, rename, remove, execute)